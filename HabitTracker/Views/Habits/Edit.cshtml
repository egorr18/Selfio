@model HabitTracker.ViewModels.HabitFormViewModel

@{
    ViewData["Title"] = "Редагувати звичку";
}

<section class="form-shell">
    <div class="section-heading">
        <div>
            <p class="eyebrow">Edit habit</p>
            <h1>Редагувати звичку</h1>
            <p>Онови назву, пріоритет, колір або категорію без втрати історії виконань.</p>
        </div>
    </div>
    <div class="data-card">
        <partial name="_HabitForm" model="Model" />
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
